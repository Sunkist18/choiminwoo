import React, { Component } from 'react'

export default class BarList extends Component {
  // set State from props (in this case, Count)
  constructor(props) {
    super(props);
  }

  render() {
    return (  
      <h1>Hello2 world</h1>
    )
  }
}
